CompileFlags:
  Add:
    - --target=arm-none-eabi
    - --sysroot=/path/to/cross-compiler/dir/with/bin/include/lib
    - -Wall
    - -Wextra
    - -Wpedantic
  CompilationDatabase: /path/to/project

---

If:
  PathMatch: /path/to/src

CompileFlags:
  Add: [-DNDEBUG, -Werror]
